tools:
  - class_name: "a2agent.tools.multimodalcode_tool.MultiModalCode_tool"
    config: 
      type: native
      timeout: 60
      code_sandbox_url: "http://0.0.0.0:16259"
    tool_schema:
      type: "function"
      function:
        name: "multimodalcode"
        description: ""
        parameters:
          type: "object"
          properties:
            answer:
              type: "string"
              description: "The model's answer to the geo3k problem, must be a digits"
          required: ["answer"]